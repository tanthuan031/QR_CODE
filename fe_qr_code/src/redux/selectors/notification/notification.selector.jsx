export const dataNotificationClientSelector = (state) => state.notification.dataNotificationClient;
// export const isDetailHistoryAskPermissionSelector = (state) => state.historyask.isDetail;

// Admin

export const dataNotificationAdminSelector = (state) => state.notification.dataNotificationAdmin;
